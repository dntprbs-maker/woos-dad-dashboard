# TODO — woos-dad-dashboard (WOO'S 아빠 대시보드)

> **TODO 관리 규칙 v2.1** (2026-08-22 확정 · 전 저장소 공통)
> - 구조는 두 섹션만 사용: `🔴 미해결`, `✅ 완료 로그`. 날짜별 섹션 헤더는 만들지 않습니다
> - 새 할 일은 `🔴 미해결` 맨 위에 항목만 추가. 등록일은 괄호로: `(등록: YYYY-MM-DD)`. 미해결에는 담당자를 적지 않습니다
> - 완료 항목은 취소선 없이 `✅ 완료 로그` 맨 위로 옮깁니다. 순서는 **날짜 → 작업자 → 내용 → 커밋**:
>   `- [x] YYYY-MM-DD, 작업자 — 항목 내용 (커밋 abc1234)`
> - **작업자를 반드시 적습니다** — 클로드코드 / 코덱스 / 별이 / 초롱이 / 반짝이 / n8n / 아빠 / 김영재 등. 여럿이면 쉼표로 나열
> - 커밋 해시가 없으면 `(커밋 abc1234)` 부분은 생략합니다
> - 완료 로그는 다른 파일로 옮기지 않고 무기한 누적합니다 (`CHANGELOG.md` 이관 규칙 폐기)
> - 같은 항목이 두 섹션에 중복 등장하지 않게 할 것
> - 작업일은 한국시간 오전 09:00부터 다음 날 오전 08:59까지입니다 (자정 넘겨 작업해도 오전 9시 전이면 전날 날짜)
> - **AI 도구가 작업했으면 커밋·푸시까지 마치고 완료 로그에 작업자와 함께 남깁니다.** 작업만 하고 커밋을 안 하면 그 PC 밖에서는 내용이 사라집니다

## 🔴 미해결

- [ ] **`index.html` 시안 데이터를 계속 공개로 둘지 판단** — 보호되는 건 노션 실데이터를 내주는 `/api/tasks`뿐이고, `index.html` 자체와 그 안에 박힌 시안(가짜) 데이터는 비밀번호 없이 누구나 볼 수 있다. 2026-08-27에 시안에 들어 있던 실제 직원 이름·작업 내용은 가짜로 교체했으므로 지금 새는 정보는 없다. 그래도 로그인 전에는 아예 빈 화면을 보여줄지, 지금처럼 시안을 보여줄지는 결정이 필요함 (등록: 2026-08-27)
- [ ] **master-project `data.json`에 이 프로젝트를 등록할지 판단** — 등록 전이라 마스터 대시보드와 `INDEX.md`에 이 프로젝트가 안 보인다. 신규 프로젝트 등록 결정은 별이 담당이라 Claude Code가 임의로 넣지 않음. 등록하기로 하면 `data.json` 추가 + `projects/woos-dad-dashboard.md` 세부 페이지 작성이 함께 필요(세부 페이지가 없으면 `INDEX.md` 링크가 깨짐). **이 항목의 원본은 `master-project/TODO.md`에 있음** — 둘 중 하나가 끝나면 나머지도 같이 정리할 것 (등록: 2026-08-27)

## ✅ 완료 로그

- [x] 2026-08-27, 클로드코드 — **`TODO.md` 신설 (규칙 v2.1 적용)** — 앞으로 할 일·한 일을 이 저장소에서 직접 기록하도록 작업 현황 파일을 만듦. 이 저장소에는 `CLAUDE.md`가 없어 규칙 전문을 파일 머리말에 직접 담았다(다른 저장소는 `CLAUDE.md`를 참고하라고만 적혀 있음). 완료 로그는 지금까지의 커밋 5건을 되짚어 채웠고, 미해결에는 판단 대기 2건을 등록 (커밋 c97728a)
- [x] 2026-08-27, 클로드코드 — **`/api/tasks` 접속 보호 + 공개 소스에서 실데이터 제거** — 점검 중 `/api/tasks`가 인증 없이 노션 실데이터 171건을 그대로 반환하는 것을 발견. 비밀번호 잠금(`api/login.js`)을 붙이고 인증 쿠키 `dash`를 발급 — 쿠키에는 비밀번호가 아니라 SHA-256 해시가 담기고 `HttpOnly`/`Secure`/`SameSite=Strict`, 유효기간 30일. 인증 응답이 Vercel 엣지 공유 캐시에 남던 `s-maxage=30`을 `private, no-store`로 교정하고, `DASHBOARD_PASSWORD` 미설정 시에는 데이터를 안 내주고 503을 반환하도록 막음. `index.html` 시안 데이터에 실제 직원 이름·작업 내용이 들어 있어 가짜로 교체. `.gitignore`도 정리해 `.env*`는 막고 `.env.example`만 예외 처리 (커밋 e8fe6bb, 9f8ba4b, a36c0ff)
- [x] 2026-08-27, 클로드코드 — **Vercel 깃허브 연동 자동배포로 전환** — `vercel link` + `vercel git connect`로 `main` 브랜치 push가 곧 재배포가 되도록 연결하고, 실제 push가 배포를 트리거하는지 실측 확인(과거 master-project-dashboard가 수동 업로드였던 걸 뒤늦게 발견한 사례가 있어 눈으로 확인함). 운영 URL: https://woos-dad-dashboard.vercel.app (커밋 490c9e3)
- [x] 2026-08-27, 클로드코드 — **저장소 초기 구축** — 아빠가 다운로드 폴더에 갖고 계시던 대시보드 파일(노션 `작업·업무협업` DB를 읽어 프로젝트별 할 일/한 일로 보여주는 화면)을 빈 깃허브 저장소에 올림. `/api/tasks`가 노션 DB를 100개씩 페이지네이션해 전부 읽고, 프로젝트명으로 묶은 뒤 상태=`완료`면 한 일·그 외는 할 일로 분류 (커밋 95e7275)
