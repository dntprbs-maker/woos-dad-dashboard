# TODO — woos-dad-dashboard (WOO'S 아빠 대시보드)

> **TODO 관리 규칙 v2.1** (2026-08-22 확정 · 전 저장소 공통)
> - 구조는 두 섹션만 사용: `🔴 미해결`, `✅ 완료 로그`. 날짜별 섹션 헤더는 만들지 않습니다
> - 새 할 일은 `🔴 미해결` 맨 위에 항목만 추가. 등록일은 괄호로: `(등록: YYYY-MM-DD)`. 미해결에는 담당자를 적지 않습니다
> - 완료 항목은 취소선 없이 `✅ 완료 로그` 맨 위로 옮깁니다. 순서는 **날짜 → 작업자 → 내용 → 커밋**:
>   `- [x] YYYY-MM-DD, 작업자 — 항목 내용 (커밋 abc1234)`
> - **작업자를 반드시 적습니다** — 클로드코드 / 코덱스 / 별이 / 초롱이 / 반짝이 / n8n / 아빠 / 김영재 등. 여럿이면 쉼표로 나열
> - 커밋 해시가 없으면 `(커밋 abc1234)` 부분은 생략합니다
> - 완료 로그는 다른 파일로 옮기지 않고 무기한 누적합니다 (`CHANGELOG.md` 이관 규칙 폐기)
> - 같은 항목이 두 섹션에 중복 등장하지 않게 할 것
> - 작업일은 한국시간 오전 09:00부터 다음 날 오전 08:59까지입니다 (자정 넘겨 작업해도 오전 9시 전이면 전날 날짜)
> - **AI 도구가 작업했으면 커밋·푸시까지 마치고 완료 로그에 작업자와 함께 남깁니다.** 작업만 하고 커밋을 안 하면 그 PC 밖에서는 내용이 사라집니다
>
> **⚠️ 2026-08-27부터 — 이 파일은 원장이 아닙니다.**
> 작업 상태의 최종 원장은 Notion `작업·업무협업` DB입니다. 이 파일은 저장소 안에서만
> 의미 있는 세부를 담는 **보조 기록**입니다. 다른 AI와 공유해야 하거나 이후 이어서
> 처리할 일은 **반드시 Notion에도** 넣고, `TODO.md`에만 적고 마감하지 않습니다.
> 규칙 전문은 전역 `~/.claude/CLAUDE.md`의 "마감 규칙 — Notion 공용 작업 원장에 반드시 기록" 참고.
> 접근은 `/api/v1`(REST) 또는 `woos-tasks` MCP — 문서는 `docs/작업관리-API.md`.

## 🔴 미해결

- [ ] **코덱스·해리를 공용 작업관리 MCP에 붙일지 확인** — 초롱이·별이·Claude Code는 2026-08-27에 붙였다. 남은 둘: **코덱스**(ChatGPT 데스크톱 앱)는 같은 ChatGPT 계정 커넥터를 물려받는지 아직 모른다 — 데스크톱 앱에서 "공용 작업관리 커넥터로 미완료 작업 3건 보여줘"로 물어보면 바로 판명된다. 사용량이 없어 **2026-08-31(월)** 이후에 확인 예정. 물려받으면 추가 작업 없음, 아니면 별도 키 필요. **해리**(헤르메스 AI, 노트북 설치본)는 어떤 앱 위에서 도는지부터 확인해야 한다 — MCP 지원 + 헤더 설정이 되면 별이와 같은 방식, stdio만 되면 `mcp-remote` 중계, MCP가 없으면 REST `/api/v1`. 둘 다 붙이기로 하면 `codex:`/`harry:` 라벨 키를 새로 만들어 `.env`와 Vercel에 넣고 재배포해야 한다 (등록: 2026-08-27)
- [ ] **`index.html` 시안 데이터를 계속 공개로 둘지 판단** — 보호되는 건 노션 실데이터를 내주는 `/api/tasks`뿐이고, `index.html` 자체와 그 안에 박힌 시안(가짜) 데이터는 비밀번호 없이 누구나 볼 수 있다. 2026-08-26에 시안에 들어 있던 실제 직원 이름·작업 내용은 가짜로 교체했으므로 지금 새는 정보는 없다. 그래도 로그인 전에는 아예 빈 화면을 보여줄지, 지금처럼 시안을 보여줄지는 결정이 필요함 (등록: 2026-08-26)
- [ ] **master-project `data.json`에 이 프로젝트를 등록할지 판단** — 등록 전이라 마스터 대시보드와 `INDEX.md`에 이 프로젝트가 안 보인다. 신규 프로젝트 등록 결정은 별이 담당이라 Claude Code가 임의로 넣지 않음. 등록하기로 하면 `data.json` 추가 + `projects/woos-dad-dashboard.md` 세부 페이지 작성이 함께 필요(세부 페이지가 없으면 `INDEX.md` 링크가 깨짐). **이 항목의 원본은 `master-project/TODO.md`에 있음** — 둘 중 하나가 끝나면 나머지도 같이 정리할 것 (등록: 2026-08-26)

## ✅ 완료 로그

- [x] 2026-08-27, 아빠, 클로드코드 — **마감 규칙 개정: 작업 원장을 Notion으로 이관** — 아빠 지시로 작업 상태의 최종 원장이 Notion `작업·업무협업` DB가 됐다. `TODO.md`·작업 메모·GitHub Issue는 보조 기록이며 원장을 대체하지 않는다. 마감 전에 완료 작업·진행 상태·새로 발견한 문제·미해결·아빠 결정이 필요한 사항·다음 작업·관련 커밋과 파일을 Notion에 반영하고, 등록 전 반드시 기존 작업을 검색해 있으면 갱신·완료 처리한다(`force`로 중복 강제 생성 금지). 세션 종료 전 5가지 점검을 통과해야 마감 완료로 본다. 규칙 전문은 전역 `~/.claude/CLAUDE.md`에 넣어 어느 프로젝트에서든 보이게 했고, 노션 쪽 규칙 페이지는 초롱이가 맡는다. **오늘치를 새 규칙대로 원장에 반영 완료** — 기존 작업 검색(미완료 67건 + 최근 완료 7건) 후 중복 없음을 확인하고 완료 3건·다음 작업 1건을 신규 등록했다. 이미 원장에 있던 판단 2건(`index.html` 공개 여부, master-project `data.json` 등록)은 새로 만들지 않고 그대로 뒀다. 5가지 점검 전부 통과, 중복 0건. 구버전 `📋 작업 관리` DB 정리는 아빠가 직접 처리
- [x] 2026-08-27, 클로드코드, 아빠 — **AI 셋을 공용 작업관리 MCP에 실제 연결 (초롱이·별이·Claude Code)** — 크롬을 띄워 화면을 같이 보며 붙였다. **초롱이(ChatGPT)**: 설정→플러그인 맨 아래 `개발자 모드`를 켜야 커스텀 MCP가 열린다(계정 전체에 적용되는 설정이라 앞으로 출처 불명 커넥터는 붙이지 말 것). 인증 선택지가 OAuth/인증 없음/혼합 셋뿐이고 **커스텀 헤더 칸이 없어서** 미리 열어 둔 경로 키 방식(`/api/mcp/<키>`)으로 붙였다. **별이(Claude Chat)**: 커넥터 메뉴가 설정→맞춤 설정으로 옮겨져 있다. 이쪽은 `요청 헤더` 칸이 있어서 URL에 키를 넣지 않고 `authorization: Bearer <키>`로 붙였다(저장 후 값이 다시 표시되지 않아 더 안전). 처음에 경로 키로 넣었다가 헤더 방식으로 다시 만들었다. 도구 정의에 넣어 둔 `readOnlyHint`/`destructiveHint` 덕에 Claude가 읽기 6개·쓰기 3개로 자동 분류했고, 아빠가 전부 자동 허용으로 설정. **Claude Code**: `claude mcp add --transport http -s user` 한 줄(헤더 방식). CLI가 PATH에 없어 `AppData/Roaming/Claude/claude-code/<버전>/claude.exe`를 직접 불렀다. 셋 다 도구 9개 인식 확인했고, 초롱이와 별이가 **같은 미완료 3건**을 돌려줘 한 원장을 공유하는 것을 실측 확인. 키는 라벨별로 따로 줬다(`chatgpt`/`claude`/`code`) — 한쪽이 새도 그 라벨만 빼면 나머지는 안 끊긴다
- [x] 2026-08-27, 클로드코드 — **Remote MCP 서버 추가 (`/api/mcp`)** — 초롱이가 작업 조회·유사검색·생성·수정을 도구로 직접 부를 수 있게 MCP 엔드포인트를 열었다. Streamable HTTP(JSON-RPC over POST), 상태 없음. 도구 9개 — `search_tasks`/`list_tasks`/`get_task`/`create_task`/`update_task`/`archive_task`/`get_schema` + ChatGPT 딥리서치가 기대하는 이름인 `search`/`fetch`. REST를 다시 HTTP로 부르는 대신 핵심 동작을 `api/_lib/ops.js`로 빼내 REST와 MCP가 **같은 코드**를 쓰게 했다 — 중복 차단·서버측 필터가 어느 경로로 들어와도 똑같이 걸린다. 운영 규칙(생성 전 검색, 중복이면 갱신, 거절당하면 force 금지)은 `initialize`의 `instructions`로 전달돼 연결하는 순간 모델이 함께 읽는다. 도구 실패는 JSON-RPC 오류가 아니라 `isError` 도구 결과로 돌려줘 모델이 중복 후보를 읽고 `update_task`로 넘어갈 수 있다. 인증은 `Authorization` 헤더가 기본이고, 헤더 칸이 없는 클라이언트를 위해 `/api/mcp/<키>` 경로 방식도 함께 연다. 로컬·프로덕션 양쪽에서 검증 전 항목 통과, 기존 REST와 대시보드 영향 없음 확인 (커밋 835f220)
- [x] 2026-08-27, 클로드코드 — **공용 작업관리 API 신설 (Notion 공식 REST API)** — 초롱이가 작업 DB를 점검하다 Notion MCP `query_data_sources`에서 `usage_limit_reached`를 맞은 게 발단. 그 한도는 Notion **MCP 서버 쪽 entitlement**이고 공식 REST API에는 없다는 걸 실측으로 확인했다(연속 20회 조회 2.8초에 전부 성공). 그래서 Notion을 원장으로 계속 쓰기로 하고, 여러 AI가 같은 원장을 공유할 공용 API `/api/v1`을 만들었다 — 조건 조회·유사 작업 검색·수정·생성이고, 생성은 내부적으로 유사검색을 먼저 돌려 중복이면 409로 막는다. 조회 조건은 전부 Notion 서버에서 필터링해 전체를 받아오지 않는다. DB 스키마를 런타임에 읽어 매핑하므로 `📋 작업 관리`와 `작업·업무협업` 어느 쪽에도 붙는다(원장은 `작업·업무협업`으로 확정). 실제 DB로 검증 전 항목 통과, 검증용 작업은 정리했고 미완료 67건으로 원상복구. 검증 중 Integration에 쓰기 권한이 없어 403이 나던 것을 아빠가 해결. 노션 쿼리 인덱스가 0.5초 늦게 반영돼 그 안에 들어온 동시 생성은 중복검사를 빠져나가는데, 실사용에서 겹칠 일이 없어 문서화만 하고 넘어감. 배포 후 `/api/v1/tasks/search`와 `/api/v1/tasks/{id}` 두 단계 경로가 Vercel 엣지에서 404 나는 걸 발견 — `[...path].js` 캐치올이 배포 환경에서 한 단계만 받았다(로컬에서는 정상이라 배포 후에야 드러남). 명시적 rewrite로 바꿔 해결하고 프로덕션에서 전 항목 재검증 완료 (커밋 bfedaef, 422dd69)
- [x] 2026-08-26, 클로드코드 — **`TODO.md` 신설 (규칙 v2.1 적용)** — 앞으로 할 일·한 일을 이 저장소에서 직접 기록하도록 작업 현황 파일을 만듦. 이 저장소에는 `CLAUDE.md`가 없어 규칙 전문을 파일 머리말에 직접 담았다(다른 저장소는 `CLAUDE.md`를 참고하라고만 적혀 있음). 완료 로그는 지금까지의 커밋 5건을 되짚어 채웠고, 미해결에는 판단 대기 2건을 등록 (커밋 c97728a)
- [x] 2026-08-26, 클로드코드 — **`/api/tasks` 접속 보호 + 공개 소스에서 실데이터 제거** — 점검 중 `/api/tasks`가 인증 없이 노션 실데이터 171건을 그대로 반환하는 것을 발견. 비밀번호 잠금(`api/login.js`)을 붙이고 인증 쿠키 `dash`를 발급 — 쿠키에는 비밀번호가 아니라 SHA-256 해시가 담기고 `HttpOnly`/`Secure`/`SameSite=Strict`, 유효기간 30일. 인증 응답이 Vercel 엣지 공유 캐시에 남던 `s-maxage=30`을 `private, no-store`로 교정하고, `DASHBOARD_PASSWORD` 미설정 시에는 데이터를 안 내주고 503을 반환하도록 막음. `index.html` 시안 데이터에 실제 직원 이름·작업 내용이 들어 있어 가짜로 교체. `.gitignore`도 정리해 `.env*`는 막고 `.env.example`만 예외 처리 (커밋 e8fe6bb, 9f8ba4b, a36c0ff)
- [x] 2026-08-26, 클로드코드 — **Vercel 깃허브 연동 자동배포로 전환** — `vercel link` + `vercel git connect`로 `main` 브랜치 push가 곧 재배포가 되도록 연결하고, 실제 push가 배포를 트리거하는지 실측 확인(과거 master-project-dashboard가 수동 업로드였던 걸 뒤늦게 발견한 사례가 있어 눈으로 확인함). 운영 URL: https://woos-dad-dashboard.vercel.app (커밋 490c9e3)
- [x] 2026-08-26, 클로드코드 — **저장소 초기 구축** — 아빠가 다운로드 폴더에 갖고 계시던 대시보드 파일(노션 `작업·업무협업` DB를 읽어 프로젝트별 할 일/한 일로 보여주는 화면)을 빈 깃허브 저장소에 올림. `/api/tasks`가 노션 DB를 100개씩 페이지네이션해 전부 읽고, 프로젝트명으로 묶은 뒤 상태=`완료`면 한 일·그 외는 할 일로 분류 (커밋 95e7275)
